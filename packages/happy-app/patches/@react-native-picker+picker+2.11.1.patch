diff --git a/node_modules/@react-native-picker/picker/android/src/main/jni/CMakeLists.txt b/node_modules/@react-native-picker/picker/android/src/main/jni/CMakeLists.txt
index 4d1eb14..c3320a9 100644
--- a/node_modules/@react-native-picker/picker/android/src/main/jni/CMakeLists.txt
+++ b/node_modules/@react-native-picker/picker/android/src/main/jni/CMakeLists.txt
@@ -17,8 +17,17 @@ add_compile_options(
   -Wno-gnu-zero-variadic-macro-arguments
 )
 
+# Copy codegen sources locally to avoid Windows 260-char path limit with Ninja
+set(_CG_LOCAL_DIR ${CMAKE_CURRENT_SOURCE_DIR}/_cg)
+file(MAKE_DIRECTORY ${_CG_LOCAL_DIR})
+file(GLOB _CG_ORIG_FILES ${LIB_ANDROID_GENERATED_COMPONENTS_DIR}/*.cpp ${LIB_ANDROID_GENERATED_COMPONENTS_DIR}/*.h)
+foreach(_f ${_CG_ORIG_FILES})
+  get_filename_component(_fn ${_f} NAME)
+  configure_file(${_f} ${_CG_LOCAL_DIR}/${_fn} COPYONLY)
+endforeach()
+
 file(GLOB LIB_CUSTOM_SRCS CONFIGURE_DEPENDS *.cpp)
-file(GLOB LIB_CODEGEN_SRCS CONFIGURE_DEPENDS ${LIB_ANDROID_GENERATED_COMPONENTS_DIR}/*.cpp)
+file(GLOB LIB_CODEGEN_SRCS CONFIGURE_DEPENDS ${_CG_LOCAL_DIR}/*.cpp)
 
 add_library(
   ${LIB_TARGET_NAME}
@@ -31,6 +40,7 @@ target_include_directories(
   ${LIB_TARGET_NAME}
   PUBLIC
   .
+  ${_CG_LOCAL_DIR}
   ${LIB_ANDROID_GENERATED_JNI_DIR}
   ${LIB_ANDROID_GENERATED_COMPONENTS_DIR}
 )
